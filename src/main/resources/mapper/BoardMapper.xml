<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyeon.demo.Repository.BoardRepository">

    <select id="findAllBoards" resultType="com.hyeon.demo.model.Board">
        SELECT id
             , title
             , content
             , createdAt
          FROM board
         WHERE 1 = 1
           AND isDeleted = 'N'
    </select>

    <select id="findOneBoard" parameterType="int" resultType="com.hyeon.demo.model.Board">
        SELECT id
             , title
             , content
             , createdAt
          FROM board
         WHERE 1 = 1
           AND id = #{boardId}
           AND isDeleted = 'N'
    </select>

    <insert id="insertBoard" parameterType="com.hyeon.demo.model.Board">
        INSERT INTO board (
            title
            , content
            , createdAt
        )
        VALUES (
            #{title}
            , #{content}
            , now()
        )
    </insert>

    <update id="updateBoard" parameterType="com.hyeon.demo.model.Board">
        UPDATE board
           SET title = #{title}
             , content = #{content}
             , updatedAt = now()
         WHERE 1 = 1
           AND id = #{id}
    </update>

    <delete id="deleteBoard" parameterType="int">
        UPDATE board
           SET isDeleted = 'Y'
         WHERE 1 = 1
           AND id = #{id}
    </delete>

</mapper>